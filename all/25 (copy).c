// construct a flowchart to show how to store the first 100 natural number in an array and then show them in the reversw sequence.
#include<stdio.h>
int main()
{
int n;
printf("enter the num");
scanf("%d",&n);
arr[n];
for(int i=0; i<n; i++)
{
printf("enter the elements");
scanf("%d",&arr[i]);
}
for (int i=0; i<=100; i++)
{

