// given an array of integers with 1 to n elements and the size of the array if n+1. one elements is occuring more than once i.e. duplicate number is present find the duplicate elements.
#include<stdio.h>
int main()
{
int n;
printf("enter the num")
scanf("%d",&n);
for(int i= 0; i<n-1; i++)
{
arr[i]=k;
printf("enter the elements")
scanf("%d",&arr[i]);
}
for(int j=j+1; j<n; j++)
{
if(arr[i]==arr[j])
{
printf("%d\n",n);
return 0;
}
}
}
*/
given an array to integers number that is already soted in one decreasing order find the two numbers such that they add up to specific target number.
#include<stdio.h>
int main()
{

