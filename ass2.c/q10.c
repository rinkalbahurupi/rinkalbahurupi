//construct a flowchart to check whether a number is the largest 4 digit number.
#include<stdio.h>
int main()
{
int num;
printf("enter the number,num");
scanf("%d",&num);
if(num>=1000)
{
printf("its largest no");
}
else
{
printf("its smallest no");
}
return 0;
}
