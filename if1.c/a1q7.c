//construct a flowchart to show obtain the are of a traingle on the basis of the and height.
#include<stdio.h>
int main()
{
int b,h,area;
printf("enter the number:a,b");
scanf("%d %d",&b,&h);
area=((0.5)*b*h);
printf("%d\n",area);
return 0;
}
