//construct a flowchart to calculate the area and perimeater of a rectangle 
#include<stdio.h>
int main()
{
int l,b,a,p;
printf("enter the number,l,b");
scanf("%d %d",&l,&b);
a=l*b;
p=2*(l+b);
printf("%d %d/n",a,p);
return 0;
}

